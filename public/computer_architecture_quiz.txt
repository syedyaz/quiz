1. What is the primary goal of instruction-level parallelism (ILP), and name two hardware techniques used to exploit it?

2. Explain the difference between a true data dependence (RAW hazard) and an anti-dependence (WAR hazard). Provide an example of each.

3. What is a control hazard, and describe one method used to reduce the performance penalty caused by branch instructions?

4. Define what a superscalar processor is and explain how it differs from a pipelined processor in terms of instruction throughput.

5. Briefly explain how out-of-order execution can improve performance while maintaining the correct program semantics.

6. What problem does register renaming solve in superscalar and out-of-order processors? Give a concrete example.

7. Explain what a vector instruction is and how it differs from scalar instructions in terms of performance efficiency for data-parallel workloads.

8. Describe the SIMD execution model and provide an example of a modern SIMD architecture or instruction set extension.

9. What is the key advantage of GPU architectures for data-level parallelism compared to CPUs? Briefly describe the thread hierarchy in GPUs.

10. Why is memory bandwidth a critical bottleneck in vector and GPU architectures, and what techniques can be used to mitigate this issue?
